/**
 * Maintenance mode configuration.
 * 
 * CRITICAL: This flag controls whether the site is accessible.
 * When true, ONLY the MaintenanceScreen is rendered - no routes, no pages, no navigation.
 * 
 * Set MAINTENANCE_ENABLED to true to show only the offline screen.
 * Set to false to restore normal app functionality.
 */
export const MAINTENANCE_ENABLED = false;
